name: Tests

on:
  push:
    branches:
      - dev
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: first step
        run: echo "tes"
